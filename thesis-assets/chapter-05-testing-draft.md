# 第五章 系统测试

## 5.1 测试目的与测试思路

系统测试的目的在于验证本系统在既定业务场景下的功能完整性、交互稳定性与基本可用性，并为后续论文结论与系统评价提供依据。由于本系统核心能力依赖外部大语言模型服务，模型生成内容本身具有一定开放性与非确定性，因此测试过程不能完全采用传统意义上的精确断言方式，而需要结合黑盒测试、边界测试与结果合理性检查，对系统的业务流程进行综合验证。

由于大语言模型应用的评价通常涉及生成质量、稳定性、鲁棒性和任务适配性等多维度指标，本文第五章的测试并不等同于完整的大模型能力评测。当前测试重点是验证本系统工程链路、业务流程和交互状态是否按照设计预期运行；模型输出质量的系统化评测可作为后续研究方向继续展开。

结合系统实现内容可知，本系统的关键功能链路包括用户身份认证、简历上传与解析、岗位模板驱动的会话创建、多轮流式面试交互以及总结性评估报告生成。其中，普通 REST 请求主要验证数据提交、权限校验和数据库写入是否正确，流式对话部分则重点关注连接建立、首字节响应、内容连续推送以及异常恢复机制是否稳定。因此，本章测试思路以“功能正确性优先，性能体验作为补充”展开，即先验证核心业务是否闭环，再通过首字节响应时间、解析耗时和流式展示效果评估系统在真实使用过程中的可接受性。

## 5.2 测试环境说明

本系统的测试环境以本地开发部署为主，主要由浏览器端、后端服务、数据库服务和外部模型调用环境构成。前端页面在浏览器中运行，后端基于 Spring Boot 启动服务，数据库采用 MySQL 8.0 存储业务数据，模型能力通过 DeepSeek API 获取。当前已采集的本机测试环境如表 5-2 所示。

### 表 5-2 测试环境表

| 项目 | 实测值 |
| --- | --- |
| 操作系统 | Microsoft Windows 11 |
| PowerShell | 7.6.1 |
| JDK | OpenJDK 21.0.4 Microsoft build |
| Maven | Apache Maven 3.9.11 |
| Node.js | v22.15.1 |
| npm | 11.6.0 |
| MySQL | MySQL Community Server 8.0.45 |
| 浏览器 | Microsoft Edge 147.0.3912.72 |
| 网络环境 | 本机回环地址访问；Demo Twin 模式下外部模型调用由脚本化数据承担 |

对于依赖外部模型服务的流式接口测试，本章将 Demo Twin 模式与真实公网模型调用区分说明。本次业务测试用于验证系统闭环和前后端交互稳定性，网络路径主要为本机回环地址；若后续切换到真实 DeepSeek 或 OpenAI 兼容模型，响应时间还会受到公网网络质量、模型服务负载和上下文长度影响。

## 5.3 功能测试

功能测试重点验证系统的五类核心业务链路是否能够按预期完成。首先，在简历上传与解析场景下，应验证系统是否能够正确接收合法 PDF 文件、完成文本提取、调用外部模型并在页面展示结构化结果；同时，还需验证对非 PDF 文件、过大文件或解析失败场景的错误提示是否有效。其次，在面试初始化场景下，应重点检查用户是否能够基于已上传简历与岗位模板成功创建面试会话，系统是否能够写入首条 `system` 消息，并在历史会话列表中正确保留对应记录。

对于流式对话场景，功能测试不仅要验证用户输入能否成功触发模型回复，更要关注前端是否能以渐进式方式展示返回内容、按钮状态是否与连接状态联动、异常情况下是否能够提示“连接已断开，请重试”等信息。由于该模块是系统最核心的交互功能，因此测试时还应重点关注断流、接口异常或空内容输入时的边界表现，避免出现页面长时间卡死、发送按钮不可恢复或数据库消息写入不完整等问题。最后，在面试结束与报告生成场景中，需要验证系统是否能根据会话历史生成总结性报告，并正确更新会话状态，使用户能够在报告页及历史记录页查看完整结果。

在模型配置验证方面，系统新增了 LLM 配置连通性测试接口，用于验证当前用户所选 Provider、模型和 API Key 是否可用。真实模式下，该接口会发起一次轻量模型调用，并根据调用结果返回测试状态；Demo 模式下，该接口返回固定可用状态，保证答辩演示链路不依赖公网模型服务。该测试项的目的不是评价模型能力，而是验证系统配置链路、密钥保存逻辑和错误提示机制是否可用。

根据当前系统实现，功能测试用例可围绕以下典型场景展开：正常 PDF 简历上传与解析、非 PDF 文件上传拦截、岗位选择后会话初始化、流式对话正常接收、结束面试后生成评估报告，以及空内容或恶意输入的拦截与提示。模型配置连通性测试可作为补充项，用于验证 Provider、模型和密钥配置链路是否可用。除此之外，出于安全性与完整性考虑，还应补充未登录访问受保护接口、跨用户访问他人简历或会话、以及 SSE 中断后前端状态恢复等测试结果。这类测试虽然不一定直接体现在页面主流程中，但在答辩问答和系统可靠性评估中具有较高重要性。

### 表 5-1 功能测试用例表

| 测试编号 | 测试模块 | 输入操作 | 预期结果 | 实际结果 | 是否通过 |
| --- | --- | --- | --- | --- | --- |
| TC-01 | 简历上传 | 上传合法 PDF 简历 | 系统完成解析并展示技能、项目结果 | 上传成功，返回 `resumeId=59`，浏览器记录接口耗时 36 ms | 通过 |
| TC-02 | 文件校验 | 上传非 PDF 文件 | 系统拒绝上传并提示文件类型错误 | 已拒绝，HTTP 400，提示“仅支持 PDF 文件” | 通过 |
| TC-03 | 面试初始化 | 选择岗位并开始面试 | 创建会话成功，写入首条 `system` 消息 | 创建成功，会话 ID 为 121 | 通过 |
| TC-04 | 流式对话 | 提交一轮用户回答 | 前端逐段接收模型回复并正常展示 | SSE 返回成功，首个 chunk 59 ms，总耗时 198 ms | 通过 |
| TC-05 | 报告生成 | 点击结束面试 | 系统生成 Markdown 报告并更新会话状态 | 报告生成成功，返回 303 字符 Markdown 内容 | 通过 |
| TC-06 | 边界输入 | 提交空内容或异常内容 | 前端拦截或后端提示错误，不进入异常状态 | 空内容提交被拦截，未造成会话异常 | 通过 |
| TC-07 | 权限校验 | 未登录访问受保护接口 | 系统拒绝访问并提示登录失效或未授权 | 已拒绝，HTTP 401，提示“请先登录” | 通过 |
| TC-08 | 数据隔离 | 尝试访问他人会话或简历 | 系统拒绝访问，不返回他人数据 | 已拒绝，HTTP 400，提示“面试会话不存在或无权访问” | 通过 |
| TC-09 | SSE 异常恢复 | 流式过程中人为断流或异常中断 | 页面提示异常，发送按钮恢复可用 | 已模拟 `AbortError`，重新发送后成功恢复 | 通过 |

除上述业务功能测试外，本轮已先完成后端单元测试和前端生产构建验证，结果如表 5-3 所示。该表只能说明工程构建与自动化测试入口可用，不能替代 `TC-01` 至 `TC-09` 的真实业务操作验证。

### 表 5-3 构建与自动化验证记录表

| 验证项 | 命令 | 实测结果 | 是否通过 |
| --- | --- | --- | --- |
| 后端单元测试 | `mvn -q test` | 命令正常结束，仅出现 Java agent warning | 通过 |
| 前端生产构建 | `npm run build` | `vue-tsc --noEmit && vite build` 成功，存在 chunk size warning | 通过 |

## 5.4 性能测试

本系统的性能测试不以大规模压测为目标，而是聚焦于真实使用场景下最关键的体验指标。对于模拟面试系统而言，用户对性能的直接感知主要集中在两个方面：一是提交回答后多久能够看到模型开始返回内容，即流式接口首字节响应时间；二是上传简历后系统需要多久才能完成文本提取与结构化解析。因此，本章性能测试建议重点记录 SSE 接口的 TTFB 和 PDF 解析耗时，并结合浏览器 DevTools 网络面板或接口测试工具进行说明。

在 SSE 接口测试中，本次通过浏览器端 `fetch` 读取 `ReadableStream` 首个 chunk 的方式记录首包时间，同时记录完整流式响应耗时。由于 Demo Twin 模式下模型回答为脚本化数据，本次结果主要用于证明前后端流式链路、连接恢复和页面状态联动可用，不代表公网模型服务的真实响应速度。

对于 PDF 解析耗时测试，本次记录的是 Demo 模式下 `/api/resume/upload` 接口从浏览器发起到收到响应的总耗时。真实模式下还会叠加 PDFBox 文本提取、模型结构化分析和网络请求耗时，因此后续若需要评价公网模型性能，应单独在真实模式下补充样本。本次性能采集结果如表 5-4 所示。

### 表 5-4 业务性能采集记录表

| 指标 | 实测值 | 采集方式 |
| --- | --- | --- |
| PDF 上传/解析接口耗时 | 36 ms | 浏览器 ResourceTiming，接口为 `/api/resume/upload` |
| SSE TTFB | 59 ms | 浏览器 `fetch` 读取 `ReadableStream` 首个 chunk |
| SSE 总耗时 | 198 ms | 浏览器 `fetch` 完整读取流式响应 |

## 5.5 测试结论

从当前系统实现和测试结果可见，本系统能够围绕用户登录、简历解析、会话创建、流式对话与评估报告生成构建完整的业务闭环。`TC-01` 至 `TC-09` 覆盖了正常流程、文件类型校验、权限校验、跨用户访问拦截和 SSE 异常恢复等关键场景，测试结果均为通过，说明系统在 Demo Twin 验收环境下已具备稳定演示和基本业务验证能力。同时，模型配置连通性接口已纳入补充验证口径，用于降低真实模式下因 Provider、模型或密钥配置错误导致核心流程中断的风险。

同时，也应客观指出本系统的性能与稳定性在一定程度上受制于第三方大语言模型服务。例如，流式接口的首字节响应时间可能会受到网络环境、模型服务负载和上下文长度变化影响而产生波动；简历解析和报告生成的最终耗时也并非完全由本地系统决定。因此，在论文结论部分更适合将本系统评价为“在当前环境下能够稳定完成核心业务流程，并在可接受时间内提供交互反馈”，而不是将其表述为具备严格实时性或高并发能力的生产级平台。

总体而言，本章测试的核心价值在于证明系统能够按照设计预期稳定运行，并识别影响体验的关键边界。当前测试已完成环境信息、构建验证、核心业务用例和基础性能指标回填，能够为第六章总结与展望提供客观依据。需要注意的是，本章性能数据来自 Demo Twin 本地回环环境，真实公网模型调用性能仍应在后续部署或真实模式联调中单独采集。
