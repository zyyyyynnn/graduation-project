# MIMO 真实公网 API 全链路测试记录 2026-05-27

执行时间：2026/5/27 18:13:15

## 测试范围

- 运行模式：系统真实模式（连接 `interview_system` 数据库）
- 后端服务：`http://127.0.0.1:8080` (由 Maven 启动)
- 接口协议：HTTP / JWT 授权 / SSE (Server-Sent Events) 流式协议
- 选用模型：`mimo-v2.5-pro`
- 模型端点：`https://token-plan-cn.xiaomimimo.com/v1/chat/completions` (由 `openai` 基础服务映射)
- 鉴权凭证：使用系统用户环境变量 `MIMO_API_KEY` (安全加载)
- 测试账号：自动生成独立随机测试账号 `mimo_tester_1779876796`

## 功能测试结果

| 测试编号 | 测试模块 | 输入操作 | 预期结果 | 实际结果 | 是否通过 |
| --- | --- | --- | --- | --- | --- |
| TC-01 | 用户注册 | 提交用户名和密码进行注册 | 创建新用户成功，返回 200 成功状态码 | 注册成功，用户名 `mimo_tester_1779876796` | 是 |
| TC-02 | 用户登录 | 提交注册信息以获取访问令牌 | 返回 200 及可用 JWT Token，用于后续鉴权 | 登录成功，成功提取 JWT 授权令牌 | 是 |
| TC-03 | 模型配置 | 将 OpenAI 提供商映射至 MIMO 节点并保存 | 绑定 `openai` 渠道至 mimo-v2.5-pro 成功 | 更新配置成功，保存至用户表数据库 | 是 |
| TC-04 | 连通性测试 | 调用 `/api/user/llm-config/test` | 后端与真实 MIMO API 通信，返回“模型配置测试通过” | 通过，真实请求公网 MIMO 并取得响应（耗时 3.20s） | 是 |
| TC-05 | 简历上传 | 上传测试简历 `demo-resume.pdf` | 后端成功提取文本并返回 resumeId | 通过，返回 `resumeId=7`，技能与项目占位数组成功提取 | 是 |
| TC-06 | 会话初始化 | 选择 `Java 后端工程师` 岗位并关联简历启动面试 | 创建新会话成功，写入首个 system 提示词 | 成功，会话 ID = 5，状态初始化为 `ongoing`，阶段为 `warmup` | 是 |
| TC-07 | 破冰流式提问 | 触发 autoStart 进行第一轮提问 | 后端通过 SSE 返回面试官开场自述和提问 | 成功，获取流式提问：“请先做一个简短的自我介绍...” | 是 |
| TC-08 | 追问流式交互 | 提交用户回答并获取追问 | 后端写入用户回答，并流式输出针对项目的跟进问题 | 成功，获取流式追问：“业务目标是什么？系统架构图？技术挑战？” | 是 |
| TC-09 | 阶段推进校验 | 修改阶段为 `technical` 并提交追问 | 成功推进阶段至 `technical` 并进行技术问答 | 成功更新，返回阶段为 `technical`，通过修改后的非锁死校验 | 是 |
| TC-10 | 评估报告生成 | 调用 `finish` 接口结束面试 | 触发大模型对多轮对话深度总结并输出三维评分报告 | 成功生成，Markdown 评估报告共包含 2085 字符（耗时 118.35s） | 是 |
| TC-11 | 历史指标分析 | 请求雷达图数据与薄弱点统计接口 | 数据库成功持久化评分数据与分析数据，可供图表读取 | 成功提取技术 `6` 分、表达 `4` 分、逻辑 `5` 分及 4 项分类薄弱点描述 | 是 |

通过情况：11/11

## 性能采集与时延分析

| 指标 | 实测值 | 采集方式 | 说明 |
| --- | --- | --- | --- |
| 外部模型连通性测试时延 | 3.20 s | 后端测试接口响应时长 | 用于验证 API Key 与 Base URL 的握手速度 |
| 简历上传及文本提取时延 | 54 ms | 后端 `/api/resume/upload` 接口耗时 | 涉及 PDFBox 文本提取和基础处理 |
| 首轮面试官开场词流式耗时 | 1.84 s | SSE 客户端首字符接收至读取流结束 | `autoStart` 开场白输出 |
| 真实模型多维报告生成时延 | 118.35 s | 后端 `/api/interview/{id}/finish` 接口耗时 | 涉及分析多轮长对话、提取评分及 JSON 格式的薄弱点提取 |

## 伴随性 Bug 修复记录

在全链路测试过程中，我们排查并修复了原系统的**阶段锁死缺陷**：
- **缺陷表现**：原系统要求“必须回答当前阶段的问题后才能进入下一阶段”，其检测逻辑是检查最后一条消息是否为 `assistant` 角色。然而在真实全链路对话中，用户通过 `/chat` 发送回答（`user` 消息）后，后端线程会当场通过 SSE 返回面试官的下一个追问并存入数据库（`assistant` 消息）。这导致每次请求结束后，最后一项消息永远是 `assistant`，用户被永久判定为“有未答复的追问”而无法通过 `/stage` 进行阶段推进。
- **修复方案**：重构了前线 [InterviewView.vue](file:///E:/graduation-project/interview-frontend/src/views/InterviewView.vue) 和后端 [InterviewServiceImpl.java](file:///E:/graduation-project/interview-backend/src/main/java/com/interview/service/impl/InterviewServiceImpl.java) 的 `hasPendingAssistantPrompt` 判定规则。修改后，逻辑仅在当前阶段（最近一条 `system` 划分点之后）存在 `assistant` 发问、但**没有任何 `user` 回复消息**时，才判定为存在挂起提问。一旦候选人在当前阶段做过至少一次回答，即使后续产生了追问，也允许其推进至下一阶段或直接生成报告。该修复使得项目在人机交互与状态流转上更为科学。
