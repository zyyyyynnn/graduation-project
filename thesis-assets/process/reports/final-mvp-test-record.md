# 完整 MVP 测试记录

## 测试时间

- 2026-04-17

## 构建验证

| 项目 | 命令 | 结果 | 说明 |
|---|---|---|---|
| 后端 | `mvn -q -DskipTests package` | 通过 | Spring Boot 后端可完成打包 |
| 前端 | `npm run build` | 通过 | Vue 类型检查与 Vite 构建通过；存在单包体积提示，不影响本地 MVP 演示 |

## 运行环境验证

| 项目 | 结果 | 说明 |
|---|---|---|
| MySQL | 通过 | 本地 MySQL 8.0.45 可连接，数据库 `interview_system` 可用 |
| 后端启动 | 通过 | Spring Boot 成功启动并监听 `8080` |
| 前端启动 | 通过 | Vite 成功启动并监听 `5173`，`/login` 返回 200 且包含 `#app` 入口 |

## 接口边界验证

本轮按用户要求不主动消耗真实 DeepSeek 成功调用，仅验证不触发模型调用的接口边界、权限、状态与数据落库。

| 测试项 | 结果 | 说明 |
|---|---|---|
| `/api/health` | 通过 | 返回 `ok` |
| 用户注册 | 通过 | 返回统一成功响应 |
| 用户登录 | 通过 | 返回 JWT token |
| 未带 Token 访问简历列表 | 通过 | 返回 HTTP 401 |
| 岗位列表 | 通过 | 返回 3 条岗位模板 |
| 非 PDF 上传 | 通过 | 返回 HTTP 400 |
| 创建面试会话 | 通过 | 返回 `sessionId` |
| system 消息初始化 | 通过 | 数据库首条消息为 `role=system`、`seq_num=0` |
| 空回答调用流式接口 | 通过 | 返回 HTTP 400，不进入模型调用 |
| 无效会话调用流式接口 | 通过 | SSE 返回 `event:error` |
| 无效会话生成报告 | 通过 | 返回 HTTP 400，不进入模型调用 |
| 已结束会话重复生成报告 | 通过 | 返回 HTTP 400，不进入模型调用 |

## 敏感信息检查

| 检查项 | 结果 | 说明 |
|---|---|---|
| API Key 隔离 | 通过 | 真实 Key 仅写入本地忽略配置文件 |
| 前端代码扫描 | 通过 | 前端源码与构建产物未写入真实 Key |
| 文档扫描 | 通过 | README 与论文资料未写入真实 Key |
| `.gitignore` | 通过 | 已忽略 `application-local.yml`、`node_modules`、`dist`、`target` 等 |

## 未执行项

- 合法 PDF 成功解析未执行，原因是会触发 DeepSeek 真实同步调用。
- 有效会话流式问答未执行，原因是会触发 DeepSeek 真实流式调用。
- 有效会话报告生成未执行，原因是会触发 DeepSeek 真实同步调用。

以上三项需要在你明确授权“少量真实模型调用”后补测，并记录真实耗时、TTFB、返回质量和截图。
